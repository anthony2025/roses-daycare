import React, { Component } from 'react'
import styles from 'styles/Carrousel.css'

export default class Carrousel extends Component {
  render () {
    return (
      <div class={styles.carrousel}>
        <div class={styles.image} />
      </div>
    )
  }
}
